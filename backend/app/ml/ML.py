"""Legacy ML training script placeholder.

The active application uses backend/app/ml/train_sample_model.py as the
canonical training entry point, and the runtime prediction path is handled by
backend/app/services/ml_service.py.
"""

from app.ml.train_sample_model import main


if __name__ == "__main__":
    main()
